.. tab-set::

   .. tab-item:: Ubuntu 20.04
      :sync: ubuntu

      As the |zu| user, execute

      .. code:: console

         zextras$ zmcontrol status

   .. tab-item:: Ubuntu 22.04
      :sync: ubuntu

      As the |zu| user, execute

      .. code:: console

         zextras$ zmcontrol status

   .. tab-item:: RHEL 8
      :sync: rhel8

      As the |zu| user, execute

      .. code:: console

         zextras$ zmcontrol status

   .. tab-item:: RHEL 9
      :sync: rhel9

      .. hint:: On RHEL 9, as the |ru| execute the new systemd
         commands that replace the :command:`zmcontrol` command (see
         the :ref:`dedicated box <rhel-systemd>`).
